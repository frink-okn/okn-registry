#+ summary: List publications and the datasets they use
#+ tags:
#+   - nasa-gesdisc-kg

PREFIX schema: <https://purl.org/okn/frink/kg/nasa-gesdisc/schema/>
SELECT *
WHERE {
	?Publication schema:USES_DATASET ?Dataset
}
LIMIT 25
